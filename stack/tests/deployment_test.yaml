suite: test deployment
templates:
  - deployment.yaml
tests:
  - it: should work
    values:
      - values.yaml
    asserts:
      - isKind:
          of: Deployment
      - isSubset:
          path: spec
          content:
            revisionHistoryLimit: 3
          any: true
      - containsDocument:
          apiVersion: apps/v1
          kind: Deployment

      #   path: metadata.name
      #   pattern: -my-chart$
      # - equal:
      #   path: spec.template.spec.containers[0].image
      #   value: nginx:latest