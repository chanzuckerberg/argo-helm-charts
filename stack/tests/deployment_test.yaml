suite: test deployment
templates:
  - deployment.yaml
tests:
  - it: should work
    # values:
    #   - ../values.yaml
    asserts:
      - isKind:
        of: Deployment
        not: true
      - contains:
        path: spec
        content:
          revisionHistoryLimit: 3
      - contains:
        path: kind
        content: Deployment
      #   path: metadata.name
      #   pattern: -my-chart$
      # - equal:
      #   path: spec.template.spec.containers[0].image
      #   value: nginx:latest