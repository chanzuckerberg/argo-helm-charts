apiVersion: v1
kind: Service
metadata:
  name: {{ include "prom2parquet.fullname" . }}
  labels:
    {{- include "prom2parquet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prom2parquet.selectorLabels" . | nindent 4 }}

